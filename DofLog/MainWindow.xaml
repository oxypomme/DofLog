<Window x:Class="DofLog.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DofLog"
        mc:Ignorable="d"
        Title="DofLog 2" Width="320" Height="240">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>
        <ListBox Name="lb_accounts" Grid.Column="0">
            <CheckBox Content="Account 1" />
            <CheckBox Content="Account 2" />
            <CheckBox Content="Account 3" />
            <CheckBox Content="Account 4" />
            <ListBox.ContextMenu>
                <ContextMenu>
                    <MenuItem Name="lb_accounts_cm_add" Header="Ajouter" />
                </ContextMenu>
            </ListBox.ContextMenu>
        </ListBox>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Button Name="btn_connect" Content="Connexion" Grid.Row="0"/>
            <StackPanel Grid.Row="1">
                <Button Name="btn_organizer" Content="Organizer" />
                <Button Name="btn_settings" Content="Paramètres" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
